# Redirect HTTP traffic for the domain to HTTPS
server {
    listen 80;
    server_name automagicdeveloper.com www.automagicdeveloper.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# Handle HTTP traffic for the IP address
server {
    listen 80;
    server_name 134.209.119.189;

    location / {
        proxy_pass http://app:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# Handle HTTPS traffic for the domain
server {
    listen 443 ssl;
    server_name automagicdeveloper.com www.automagicdeveloper.com;

    ssl_certificate /etc/letsencrypt/live/automagicdeveloper.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/automagicdeveloper.com/privkey.pem;

    location / {
        proxy_pass http://app:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
